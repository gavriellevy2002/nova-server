# ✦ שרת נובה — מדריך הקמה (שלב 1)

בסוף התהליך תהיה לך נובה בענן, זמינה תמיד מהטלפון ומהמק, שיודעת:
לחפש באינטרנט באמת, לחקור ולשלוח לך תוצאות למייל, לתזמן משימות שרצות לבד ("כל בוקר ב-7…"), ולזכור הכל.

זמן: כ-20 דקות, פעם אחת. הכל מהטלפון או מהמק — כפי שנוח לך.

---

## שלב 1 — מפתחות שצריך להכין (5 דק')

**א. מפתח Anthropic** — כבר יש לך. אם לא: console.anthropic.com ← API Keys ← Create Key ← Never.

**ב. סיסמת אפליקציה של Gmail** (כדי שנובה תשלח מיילים):
1. חובה שיהיה לך אימות דו-שלבי מופעל בחשבון Google.
2. גש ל-**myaccount.google.com/apppasswords**
3. תן שם `nova` ← Create ← תקבל 16 תווים. **העתק אותם** (זו לא סיסמת הגוגל הרגילה!).

**ג. קוד סודי משלך** — תמציא מחרוזת קשה לניחוש, למשל `milan-tiger-92-x`. זה מה שיגן על נובה שרק אתה תשתמש בה.

---

## שלב 2 — להעלות לענן (Render, 10 דק')

Render מריץ שרתים והם נוחים לטלפון. יש תוכנית חינם, אבל היא "נרדמת" — לעוזר אישי מומלץ המסלול הקטן (~$7/חודש) שנשאר ער תמיד.

1. גש ל-**github.com** ← הירשם ← צור repository חדש בשם `nova-server` ← **Public**.
2. העלה אליו את כל הקבצים מהתיקייה הזאת (uploading an existing file → גרור את כל הקבצים).
3. גש ל-**render.com** ← הירשם עם GitHub ← **New ← Web Service** ← בחר את `nova-server`.
4. Render יזהה את ההגדרות לבד (יש קובץ `render.yaml`). לחץ **Apply / Create**.
5. במסך **Environment** הוסף את המשתנים (Add Environment Variable):
   - `ANTHROPIC_API_KEY` = המפתח שלך
   - `NOVA_TOKEN` = הקוד הסודי שהמצאת
   - `OWNER_EMAIL` = המייל שלך
   - `SMTP_USER` = המייל שלך (Gmail)
   - `SMTP_PASS` = 16 התווים מסיסמת האפליקציה
6. **Create Web Service**. חכה 2–3 דקות עד שכתוב **Live**.
7. תקבל כתובת כמו `https://nova-agent.onrender.com` — **זו נובה שלך.**

---

## שלב 3 — להשתמש (מהטלפון והמק)

1. פתח את הכתובת בדפדפן.
2. יופיע מסך שמבקש **קוד גישה** — הדבק את ה-`NOVA_TOKEN`. זהו, אתה בפנים.
3. באייפון: כפתור שיתוף ⬆ ← **הוסף למסך הבית** ← אייקון של אפליקציה.

**דברים לנסות:**
- "תחקרי מה שטיפת המכוניות הכי טובה בלייקווד ותשלחי לי למייל סיכום"
- "כל יום ראשון עד חמישי ב-7 בבוקר תשלחי לי מייל עם מזג האוויר ומה חשוב היום"
- "תזכרי שיום הנישואין שלי ב-14 במרץ ושאשתי אוהבת פרחי כלנית"

מיילים לאנשים שכבר התכתבת איתם — יוצאים לבד. מייל ראשון לאדם חדש — ממתין לאישור שלך (למעלה יופיע "⚠ מיילים לאישור").

---

## עלויות
Anthropic לפי שימוש (~$5–15/חודש) · Render ~$7/חודש למסלול הער · Gmail ו-Tavily חינם.

## מה בשלב הבא (2 ו-3)
קריאת תיבת המייל שלך + תשובות · יומן גוגל · דפדפן אמיתי שנובה לוחצת בו בעצמה.
